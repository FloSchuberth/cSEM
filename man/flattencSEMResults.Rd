% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zz_utils.R
\name{flattencSEMResults}
\alias{flattencSEMResults}
\title{Internal: Flatten a cSEMResults object}
\arguments{
\item{.object}{An object of class \link{cSEMResults} or any (nested) named
list or S4 object to be flattened.}

\item{.path}{Character string. The access path of the current element.
Used internally for the recursion; users should rely on the default.}
}
\value{
A named \code{list} with one element per leaf of \code{.object}. The names
give the full \code{"$"}-separated access path to each leaf.
}
\description{
Recursively traverses a \link{cSEMResults} object (or any nested named list /
S4 object) and returns a flat, named list whose names are the full access
paths to each leaf element. This is primarily a helper for regression
testing: comparing two flattened objects element-by-element makes it
possible to trace exactly \emph{which} result has changed between two versions
of \pkg{cSEM} instead of only learning \emph{that} the objects differ.
}
\details{
A "leaf" is any \code{NULL}, atomic vector/matrix, factor, function, or
\code{formula}. All other list-like or S4 objects are descended into.
Path components are separated by \code{"$"} and follow the same notation used
to address elements of a \link{cSEMResults} object, e.g.
\code{"Estimates$Path_estimates"}.
}
\keyword{internal}
