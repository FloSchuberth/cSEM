% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postestimate_doTrees.R
\name{doTrees}
\alias{doTrees}
\title{Recursive Partitioning Algorithm for \emph{csem} GSCA Models}
\usage{
doTrees(
  .object,
  .covariates,
  .influence = "mat",
  .splitter = c("native", "FIT", "DLi", "DGi"),
  .control = igsca_tree_control()
)
}
\arguments{
\item{.object}{A single-group \code{cSEMResults} object, as returned by \code{\link[=csem]{csem()}}.
Its data must contain the \code{.covariates} columns; \code{\link[=csem]{csem()}} ignores
non-indicator columns, so they can simply ride along in the original call.}

\item{.covariates}{Character vector of columns of \code{.object}'s data to
partition on. Must be numeric, ordered or unordered factors, and completely
observed: a missing value is refused rather than routed, because with
surrogate splits, \code{\link[partykit:ctree]{partykit::ctree()}} assigns such a row by a random
draw that is not repeated between growing the tree and recording its
fitted partition. Impute or subset before fitting \code{.object}.}

\item{.influence}{Node statistic driving variable selection.}

\item{.splitter}{Cutpoint rule. One of "native", "FIT", "DLi" or "DGi".}

\item{.control}{Tuning parameters, see \code{\link[=igsca_tree_control]{igsca_tree_control()}}.}
}
\value{
A tree of class \code{c("igsca_tree", "constparty", "party")}, carrying the
per-leaf fits (see \code{\link[=coef.igsca_tree]{coef.igsca_tree()}}) and an \code{igsca_info} attribute of
failure counters and root criteria.
}
\description{
Grows a tree that partitions the rows \code{.object} was fitted on into subgroups
whose model estimates differ. Every node is refit by replaying \code{.object}'s own
\code{\link[=csem]{csem()}} arguments, so the estimator, modes and convergence settings of the
tree are those of the fit it was given.
}
\details{
\code{.influence} chooses the statistic that will be permuted (the
conditional-inference or COIN procedure) to find which covariate is
significantly associated with the transformed 'Y' variable. Currently only
\code{"mat"} is supported: the transformed 'Y' is the casewise GSCA
squared-residual matrix \insertCite{Hwang2021a}{cSEM}.

\code{.splitter} then chooses the cutpoint within the selected covariate.
\code{"native"} is the \code{\link[partykit:ctree]{partykit::ctree()}} default, which maximizes the influence
statistic. \code{"FIT"}, \code{"DLi"} and \code{"DGi"} instead refit a two-group GSCA model
on every candidate partition and maximize either the FIT \emph{difference}, or
the \code{"DLi"}/\code{"DGi"} matrix \emph{distance}, between the pooled and multigroup
model-implied covariance matrices.
}
\references{
\insertAllCited{}
}
