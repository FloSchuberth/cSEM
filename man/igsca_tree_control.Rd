% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_doTrees.R
\name{igsca_tree_control}
\alias{igsca_tree_control}
\title{Tuning parameters for \code{\link[=doTrees]{doTrees()}}}
\usage{
igsca_tree_control(
  alpha = 0.05,
  bonferroni = TRUE,
  minbucket = 30L,
  minsplit = 2L * minbucket,
  minprob = 0.01,
  maxdepth = 3L,
  R_test = 9999L,
  coin_distribution = c("approximate", "asymptotic"),
  intersplit = FALSE
)
}
\arguments{
\item{alpha}{Significance level a covariate must clear to split a node.}

\item{bonferroni}{Adjust the node's p-values for the number of covariates
tested. The adjustment partykit applies is Šidák, \eqn{1 - (1 - p)^k}, which
agrees with Bonferroni to first order. \code{k} is the number of covariates that
returned a p-value at that node, not the number requested. The engine
applies it to whatever the selector returns. Defaults to \code{TRUE}, matching
\code{\link[partykit:ctree_control]{partykit::ctree_control()}}: every node scans all covariates, so the
unadjusted p-value is a multiple-comparison statistic reported as if it
were a single test.

Note that "Šidák" is our name for it, not partykit's -- the word appears
nowhere in that package's documentation, which calls the option
"Bonferroni" throughout. \code{?partykit::ctree_control} documents only the
choice ("\code{Bonferroni} and \code{Univariate} relate to p-values from the
asymptotic distribution (adjusted or unadjusted)"), and
\code{?partykit::ctree} only that the criterion is multiplicity adjusted. The
exponential form is visible solely in the source of partykit's unexported
\code{.extree_node()}, which multiplies the stored \eqn{\log(1 - p)} by \code{k} --
i.e. \eqn{\log((1 - p)^k)}. The primary reference for the framework is
Hothorn, Hornik and Zeileis (2006), listed under \code{?partykit::ctree}.}

\item{minbucket}{Minimum number of observations in a terminal node. With many
indicators the default admits leaves the model may not fit; see the
\code{n_fail_leaf} counter on the returned tree.}

\item{minsplit}{Minimum number of observations required to attempt a split.}

\item{minprob}{Minimum size of a terminal node as a proportion of its
parent's. partykit raises \code{minbucket} per node rather than replacing it --
\code{.extree_node()} computes \eqn{\max(\code{minbucket}, \lceil n_{node}
\times \code{minprob} \rceil)} and hands the raised value to the variable
selector and the cutpoint rule alike, so it constrains the non-native
splitters through \code{candidate_partitions()} as well as partykit's own scan.
Note that the proportion is of the \emph{node}, not of the sample: at
\code{minprob = 0.25} a depth-2 leaf can hold a sixteenth of the data. Defaults
to \code{0.01}, matching \code{\link[partykit:ctree_control]{partykit::ctree_control()}}; with the default
\code{minbucket = 30L} it does not bind below n = 3000.}

\item{maxdepth}{Maximum depth of the tree, the root counting as 0.}

\item{R_test}{Number of permutations libcoin draws for the
conditional-inference variable-selection test. Drawn only under
\code{coin_distribution = "approximate"}; ignored entirely under
\code{"asymptotic"}.}

\item{coin_distribution}{How the null distribution of the permutation
statistic is evaluated: \code{"approximate"} draws \code{R_test} permutation
resamples, \code{"asymptotic"} uses its limiting distribution. See
\code{?partykit::ctree_control()} and \code{?libcoin::LinStatExpCov}.}

\item{intersplit}{Where to report the break of a \emph{numeric} covariate. \code{FALSE} is the default in partykit and \code{TRUE} is the default for semtree.}
}
\value{
A named \code{list} of tuning parameters.
}
\description{
\code{doTrees()} runs one algorithm --
conditional-inference variable selection from libcoin, followed by a
cutpoint rule chosen with \code{.splitter} -- so every parameter below applies to
every configuration.
}
\seealso{
\code{\link[=doTrees]{doTrees()}}
}
