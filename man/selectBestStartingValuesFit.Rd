% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_foreman.R
\name{selectBestStartingValuesFit}
\alias{selectBestStartingValuesFit}
\title{Internal: Fit GSCA from several starting-value sets and keep the best FIT}
\usage{
selectBestStartingValuesFit(.candidates, .args_used)
}
\arguments{
\item{.candidates}{A list of \code{.starting_values} sets (the multi-start form).}

\item{.args_used}{The captured \code{foreman()} argument list to reuse per fit.}
}
\value{
A \code{foreman()} output list (the best-FIT fit) augmented with a
\code{Information$Weight_info$Multistart} record.
}
\description{
Re-invokes \code{\link[=foreman]{foreman()}} once per candidate starting-value set (each a valid
single \code{.starting_values} set), scores each admissible fit with
\code{\link[=calculateFIT]{calculateFIT()}}, and returns the fit with the highest FIT. Candidates whose
fit errors or yields a non-finite FIT are skipped (with a warning); if the
selected fit is inadmissible a warning is issued. The returned object keeps
the full candidate list in \code{Information$Arguments$.starting_values} so that
downstream resampling re-runs the multi-start on every replication.
}
\keyword{internal}
