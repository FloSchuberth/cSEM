# Internal: Function that search for cycles

Function that searches for cycles in a binary indicator matrix using the
depth first search (DFS).

## Usage

``` r
checkCycles(.matrix)
```

## Value

Logical vector `'TRUE'`: Cycle detected; `'FALSE'` No cycle detected
